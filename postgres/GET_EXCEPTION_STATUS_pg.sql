DROP FUNCTION IF EXISTS public."SP_GET_EXCEPTION_STATUS"();

CREATE OR REPLACE FUNCTION public."SP_GET_EXCEPTION_STATUS"()
RETURNS TABLE("NAME" text)
LANGUAGE sql
AS $$
    SELECT "NAME"
    FROM public."EXCEPTION_STATUS"
    ORDER BY "SORT_ORDER" ASC, "NAME" ASC;
$$;
