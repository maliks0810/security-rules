DROP FUNCTION IF EXISTS public."GET_PRIORITY_TYPE"();

CREATE OR REPLACE FUNCTION public."GET_PRIORITY_TYPE"()
RETURNS TABLE("NAME" text)
LANGUAGE sql
AS $$
    SELECT "NAME"
    FROM public."EXCEPTION_PRIORITY_TYPE"
    ORDER BY "SORT_ORDER" ASC, "NAME" ASC;
$$;
